/**
 * Future: central routing for character handlers. App currently imports
 * `src/characters` directly; this type keeps naming aligned with project rules.
 */
export type CharacterRouteId =
  | 'synergy'
  | 'ninja'
  | 'group'
  | 'meme'
  | 'scapegoat'
  | 'growth'
